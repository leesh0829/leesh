# Database
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DBNAME?sslmode=require

# Auth
NEXTAUTH_SECRET=change-this-to-a-long-random-secret
NEXTAUTH_URL=http://localhost:3000
APP_URL=http://localhost:3000

# Mail (required for sign-up verification in production)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=mailer@example.com
SMTP_PASS=change-this
SMTP_FROM=Leesh <mailer@example.com>

# Optional
LEESH_PASSWORD=change-this
APP_SECRET=optional-fallback-secret
