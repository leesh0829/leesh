import MiniGameClient from './MiniGameClient'

/**
 * Page component that renders the MiniGameClient UI.
 *
 * @returns A JSX element containing the MiniGameClient component.
 */
export default function MiniGamePage() {
  return <MiniGameClient />
}
