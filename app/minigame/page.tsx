import MiniGameClient from './MiniGameClient'

/**
 * Page component that renders the mini-game client.
 *
 * @returns A JSX element that mounts the `MiniGameClient` component.
 */
export default function MiniGamePage() {
  return <MiniGameClient />
}
